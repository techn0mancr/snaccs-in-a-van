/* Import required types */
import {Request, Response} from "express";

/* Adds the given snack to the customer's order */
function addSnackToOrder(req: Request, res: Response): void {

}

/* Export functions */
export {
    addSnackToOrder
};
